{
  "name": "aakha-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Android wrapper for the Aakha phone client (thin client; laptop runs the pipeline).",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "jsqr": "^1.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
